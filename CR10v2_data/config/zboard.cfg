[mcu zboard]
serial: /dev/serial/by-path/pci-0000:00:14.0-usb-0:2.7.7:1.0-port0
restart_method: command

#[temperature_sensor zboard]
#sensor_type: temperature_mcu
#sensor_mcu: zboard
#sensor_temperature1: 25
#sensor_adc1: 0.210317
#min_temp: 0
#max_temp: 105

[stepper_z]
#front zaxis
step_pin: zboard: PC7 #PL3
dir_pin: zboard: PC9 #PL1
enable_pin: !zboard: PC8 #PK0
microsteps: 16
rotation_distance: 8
endstop_pin: probe:z_virtual_endstop
position_min: -3
position_max: 300
homing_speed: 2

[stepper_z1]
#right side Xstepper
step_pin: zboard: PC12 #PF0
dir_pin: zboard: PB3 #PF1
enable_pin: !zboard: PD2 #PD7
microsteps: 16
rotation_distance: 8
#   See the "stepper" section for the definition of the above parameters.
#endstop_pin:

[stepper_z2]
#left side Yaxis
step_pin: zboard: PC11 #PF6
dir_pin: zboard: PA15 #PF7
enable_pin: !zboard: PC10 #PF2
microsteps: 16
rotation_distance: 8
#   See the "stepper" section for the definition of the above parameters.
#endstop_pin:


[z_tilt]
z_positions:
 155,-23 #155,-42
 329,274 #329,255
 -21.5,274 #-21.5,255
#   A list of X, Y coordinates (one per line; subsequent lines
#   indented) describing the location of each bed "pivot point". The
#   "pivot point" is the point where the bed attaches to the given Z
#   stepper. It is described using nozzle coordinates (the X, Y position
#   of the nozzle if it could move directly above the point). The
#   first entry corresponds to stepper_z, the second to stepper_z1,
#   the third to stepper_z2, etc. This parameter must be provided.
points:
 183.4,0 #155,50
 320,252 #290,314.4
 49,252 #20,314.4
 
#   A list of X, Y coordinates (one per line; subsequent lines
#   indented) that should be probed during a Z_TILT_ADJUST command.
#   Specify coordinates of the nozzle and be sure the probe is above
#   the bed at the given nozzle coordinates. This parameter must be
#   provided.
speed: 200
#   The speed (in mm/s) of non-probing moves during the calibration.
#   The default is 50.
horizontal_move_z: 7.5
#   The height (in mm) that the head should be commanded to move to
#   just prior to starting a probe operation. The default is 5.
retries: 10
#   Number of times to retry if the probed points aren't within
#   tolerance.
retry_tolerance: 0.02
#   If retries are enabled then retry if largest and smallest probed
#   points differ more than retry_tolerance. Note the smallest unit of
#   change here would be a single step. However if you are probing
#   more points than steppers then you will likely have a fixed
#   minimum value for the range of probed points which you can learn
#   by observing command output.